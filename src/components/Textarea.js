import glamorous from 'glamorous'
import { space, width, fontSize, color } from 'styled-system'

const Textarea = glamorous.textarea(space,  width, fontSize, color, {

})

Textarea.defaultProps = {

}

export default Textarea
