import glamorous from 'glamorous'
import { space, width, fontSize, color } from 'styled-system'

const Fieldset = glamorous.fieldset(space,  width, fontSize, color, {

})

Fieldset.defaultProps = {

}

export default Fieldset
