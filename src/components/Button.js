import glamorous from 'glamorous'
import { space, width, fontSize, color } from 'styled-system'

const Button = glamorous.button(space,  width, fontSize, color, {

})

Button.defaultProps = {

}

export default Button
