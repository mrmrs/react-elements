import glamorous from 'glamorous'
import { space, width, fontSize, color } from 'styled-system'

const Input = glamorous.input(space,  width, fontSize, color, {

})

Input.defaultProps = {

}

export default Input
