import glamorous from 'glamorous'
import { space, width, fontSize, borders, color } from 'styled-system'

const Input = glamorous.input(space, width, fontSize, color, borders, {

})

Input.defaultProps = {

}

export default Input
